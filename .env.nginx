# Nginx Configuration - AI Orchestrator v7.0
# These variables are used by scripts/start-nginx.sh

# Backend host - can be IP address or Docker service name
# Development (localhost): 10.10.10.46 or 127.0.0.1
# Docker Compose: backend (service name)
BACKEND_HOST=10.10.10.46

# Backend port (default: 8001)
BACKEND_PORT=8001
